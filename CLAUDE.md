# CLAUDE.md

このファイルは、このリポジトリで作業する Claude Code（および開発者）向けのガイドです。

## プロジェクト概要

`task-board` — タスク管理アプリケーション。

- リポジトリ: https://github.com/D-westside63/task-board.git
- 技術スタック: React 18 + Vite 5（JavaScript / JSX）
- 状態管理: React `useState` のみ。タスクは `localStorage` に永続化（キー: `task-board.tasks`）
- 現状の機能:
  - テキスト入力でタスクを追加
  - チェックボックスで完了／未完了を切り替え
  - タスクを削除
  - 完了済みタスクはグレー＋取り消し線で表示

## Git 運用ルール

**コードを変更するたびに、必ず GitHub（`origin`）へプッシュする。**

1. 意味のある単位で変更したら、その都度コミットする。
   - コミットメッセージは日本語で簡潔に。何を・なぜ変えたかを書く。
2. コミット後は**毎回** `git push origin <ブランチ名>` を実行し、ローカルとリモートを常に同期させる。
   - 「後でまとめてプッシュ」はしない。作業を中断する時点で未プッシュのコミットが残らないようにする。
3. 作業ブランチ運用:
   - `main` は常にビルド・動作する状態を保つ。
   - 機能追加・修正は `feature/xxx` や `fix/xxx` ブランチを切って行い、レビュー後に `main` へマージする。
   - 小さな個人プロジェクトとして `main` に直接コミットする場合も、プッシュは毎回行う。
4. プッシュ前の最低限の確認:
   - `git status` で意図しないファイルが含まれていないか確認する。
   - ビルド／テストがあればローカルで通してからプッシュする。
5. 機密情報（APIキー、`.env`、認証情報）はコミットしない。`.gitignore` で除外する。

### 典型的なフロー

```bash
git add -A
git commit -m "タスクカードのドラッグ&ドロップを実装"
git push origin main
```

## 開発コマンド

- セットアップ: `npm install`
- 開発サーバー起動: `npm run dev`（http://localhost:5173）
- 本番ビルド: `npm run build`（`dist/` に出力）
- ビルド結果の確認: `npm run preview`
- テスト: 未整備
- Lint / フォーマット: 未整備

## ディレクトリ構成

```
index.html          エントリHTML
vite.config.js       Vite 設定
src/
  main.jsx          React マウント
  App.jsx           アプリ本体（タスクの状態・追加・切替・削除）
  App.css           アプリのスタイル（完了タスクのグレー表示を含む）
  index.css         グローバルスタイル
```

## コーディング方針

- 既存コードのスタイル（命名・コメント量・構成）に合わせる。
- 変更は小さく、レビューしやすい単位に保つ。
- 公開APIや挙動を変える場合はこのファイルと関連ドキュメントも更新する。
